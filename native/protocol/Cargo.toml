[package]
name = "tokenmeter-protocol"
version = "0.2.0"
edition = "2021"
description = "Wire types shared by the TokenMeter client and the Token League server"
license = "MIT"
publish = false

[dependencies]
schemars = "1"
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
